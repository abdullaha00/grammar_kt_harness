\begin{figure*}[t]
  \centering
  \begin{tikzpicture}[
    >=Latex,
    node distance=3.5mm,
    box/.style={draw, rounded corners=1.5pt, align=center, minimum height=8mm,
      text width=2.0cm, font=\scriptsize, inner sep=2pt},
    model/.style={box, fill=orange!16, dashed},
    fixed/.style={box, fill=blue!10},
    evidence/.style={box, fill=violet!10},
    kc/.style={box, fill=green!12},
    arrow/.style={->, semithick}
  ]
    \node[fixed] (source) {grammar\\resource};
    \node[model, right=of source] (norm) {resource-specific\\normalization};
    \node[fixed, right=of norm] (cells) {canonical\\GrammarCells};
    \node[model, right=of cells] (gen) {$N=3$ item\\generation};
    \node[model, right=of gen] (valid) {independent\\screening};
    \node[fixed, very thick, right=of valid] (bank) {fixed\\item bank};

    \node[fixed, below=8mm of source] (fold) {semantic\\grammar fold};
    \node[evidence, right=of fold] (events) {development\\learner evidence};
    \node[kc, right=of events] (cands) {KC candidates\\+ support};
    \node[kc, right=of cands] (select) {predictive--\\parsimonious selection};
    \node[fixed, very thick, right=of select] (policy) {frozen policy\\+ Q-matrix};
    \node[evidence, right=of policy] (eval) {KT + frozen-probe\\evaluation};

    \draw[arrow] (source) -- (norm);
    \draw[arrow] (norm) -- (cells);
    \draw[arrow] (cells) -- (gen);
    \draw[arrow] (gen) -- (valid);
    \draw[arrow] (valid) -- (bank);
    \draw[arrow] (bank.south) |- (fold.east);
    \draw[arrow] (fold) -- (events);
    \draw[arrow] (events) -- (cands);
    \draw[arrow] (cands) -- (select);
    \draw[arrow] (select) -- (policy);
    \draw[arrow] (policy) -- (eval);
  \end{tikzpicture}
  \caption{Active \system{} methodology.  Model assistance is confined to
  normalization, item generation, and item screening.  Candidate construction
  uses development grammar only; selection uses development responses only.
  Thick boxes mark the bank and policy freezing boundaries.}
  \label{fig:pipeline}
\end{figure*}
