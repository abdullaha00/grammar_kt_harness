\begin{figure*}[t]
  \centering
  \begin{tikzpicture}[
    >=Latex,
    node distance=2.5mm and 4.5mm,
    box/.style={draw, rounded corners=1.5pt, align=center, minimum height=8mm,
      text width=2.05cm, font=\scriptsize, inner sep=2.5pt},
    linguistic/.style={box, fill=blue!10},
    truth/.style={box, fill=green!14, very thick},
    measurement/.style={box, fill=orange!16},
    observable/.style={box, fill=violet!12},
    hypothesis/.style={box, fill=gray!14, dashed},
    arrow/.style={->, semithick},
    label/.style={font=\scriptsize\bfseries, align=right, text width=1.55cm}
  ]
    \node[label] (l1) {linguistic\\structure};
    \node[linguistic, right=of l1] (resource) {grammar resource\\+ explicit scope};
    \node[linguistic, right=of resource] (cells) {canonical\\\grammarcell{}s};
    \node[truth, right=of cells] (kstar) {declared generator\\$\kstar+\qstar$};

    \node[label, below=5mm of l1] (l2) {measurement\\and platform};
    \node[measurement, right=of l2] (item) {visible item:\\format, context,\\answer space};
    \node[measurement, right=of item] (policy) {assignment policy:\\curriculum, review,\\adaptation};
    \node[truth, right=of policy] (learner) {latent learner:\\mastery +\\heterogeneity};
    \node[observable, right=of learner] (response) {observable response:\\correctness +\\optional error};

    \node[label, below=5mm of l2] (l3) {inference\\and evaluation};
    \node[hypothesis, right=of l3] (model) {model hypothesis\\$\khat+\qhat$ + KT};
    \node[observable, right=of model] (prediction) {prediction,\\calibration,\\state estimate};
    \node[truth, right=of prediction] (oracle) {private oracle:\\true Q, mastery,\\failed-KC draw};
    \node[measurement, right=of oracle] (claims) {bounded claims +\\collection design};

    \draw[arrow] (resource) -- (cells);
    \draw[arrow] (cells) -- (kstar);
    \draw[arrow] (item) -- (response);
    \draw[arrow] (policy) -- (response);
    \draw[arrow] (learner) -- (response);
    \draw[arrow] (kstar) -- (learner);
    \draw[arrow] (cells.south) |- (item.north);
    \draw[arrow] (kstar.south) |- (item.north east);
    \draw[arrow] (response.south) |- (model.east);
    \draw[arrow] (model) -- (prediction);
    \draw[arrow] (prediction) -- (oracle);
    \draw[arrow] (oracle) -- (claims);
  \end{tikzpicture}
  \caption{Known latent truth and observable measurement are different
  layers.  Responses arise jointly from learner state, the visible instrument,
  and the platform policy.  Model hypotheses consume observable histories;
  private oracle fields are opened only for declared evaluation.  Full-v1
  fixes the green truth but idealises the orange measurement layer.}
  \label{fig:pipeline}
\end{figure*}
