Re-evaluate one partial Phase-1 mapping using examples as additional evidence.

Only dimensions named after `phase2 eligible:` may change. All other cell
constraints and branch correlations remain fixed. Examples are illustrative,
not exhaustive: refine an eligible dimension only when the descriptor licenses
the broader space and the examples positively establish the refinement. Do not
infer absent alternatives or incidental grammar.

If an example directly contradicts an exact Phase-1 scalar, return unresolved
with no cells and explain the contradiction. Otherwise retain the eligibility
note as provenance.

Return one JSON object with exactly: source_id, result, cells, note.

DESCRIPTOR EVIDENCE:
{{descriptor}}

PHASE-1 MAPPING:
{{phase1_mapping}}

ELIGIBLE EXAMPLES:
{{examples}}

CANONICAL GRAMMAR SCHEMA:
{{canonical_schema}}

EGP NORMALISATION RULEBOOK:
{{rulebook}}
