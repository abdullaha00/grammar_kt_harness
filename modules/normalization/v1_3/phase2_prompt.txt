Re-evaluate one Phase-1 EGPMapping using its EGP examples as additional
evidence.

Use only the supplied descriptor, unchanged Phase-1 mapping, and examples.
Follow the same frozen schema, scope, and normalization rules.

Only dimensions explicitly named by Phase 1 after `phase2 eligible:` may be
changed. Every other constraint, branch correlation, and scalar remains fixed.
If eligibility is `none`, return Phase 1 unchanged unless an exact scalar is
directly contradicted.

Examples are illustrative, not exhaustive. Do not narrow from frequency,
agreement, or absence. Do not expand a superclass into member-wise support.

Refine a named dimension only when the descriptor licenses its closed broader
space and examples positively establish distinct alternatives without false
exhaustiveness. Otherwise leave it unchanged. Never infer Cartesian
combinations or add incidental grammar.

A successfully refined dimension may become scalar in every resulting branch
while its Phase-1 eligibility note remains unchanged as provenance. Except for
the contradiction outcome, preserve that note byte-for-byte.

If relevant examples directly contradict an exact Phase-1 scalar, return
`result=unresolved`, `cells=[]`, and a note beginning
`phase2 contradiction: <dimension>=<value>; evidence: `. Do not broaden or
replace the scalar.

Do not use KC, item-generation, or KT considerations.

Return exactly one revised EGPMapping object and no other text.

DESCRIPTOR:
{record}

PHASE 1 MAPPING:
{phase1_mapping}

EXAMPLES:
{examples}

