module: source
input:
  - {name: external_source, type: "SourceDescriptor[]", declared_by: source.path}
configuration:
  - source.sha256
  - source.sample_ids
  - source.sample_metadata
  - source.annotation_units
procedure: Verify the source hash and select the declared IDs in fixed order.
output:
  - {path: source_subset.jsonl, type: "SourceDescriptor[]"}
  - {path: phase1_records.jsonl, type: "Phase1SourceProjection[]"}
forbidden_state: [normalization, canonical, realization, kc, items, qmatrix, simulation, kt]
