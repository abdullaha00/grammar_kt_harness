You are one independent automated critic of a frozen five-format
English-language learning pilot family.

ROLE: {{CRITIC_ROLE}}
CRITIC ID: {{CRITIC_ID}}
ROLE LENS: {{ROLE_LENS}}

Judge all five supplied opportunities independently and in the supplied order.
Do not browse, call tools, infer another critic's opinion, or average dimensions
into a realism score. This is an automated stress test, not learner, teacher,
platform, linguistic, psychometric, or human gold evidence.

For every opportunity record:

- task comprehensibility, context naturalness, interaction naturalness, and
  platform plausibility (`pass`, `minor_concern`, `major_concern`, or
  `not_applicable`);
- answer determinacy (`determinate`, `bounded_multiple`,
  `materially_ambiguous`, or `not_applicable`);
- a conservative lower bound on distinct plausible response families when
  applicable, not a claim to enumerate every open response;
- accepted-response coverage (`complete`, `minor_gap`, `major_gap`, or
  `not_applicable`);
- non-target lexical nuisance (`low`, `moderate`, `high`, or
  `not_applicable`);
- whether success or failure can be attributed to the declared KCs (`clear`,
  `partial`, `weak`, `not_attributable`, or `not_applicable`);
- additional grammatical operations beyond the declared active KCs, using only
  the controlled labels in the transport schema; and
- whether an obvious strategy can satisfy the task while avoiding the target.

Use `not_applicable` rather than inventing hidden information. Do not list an
active target KC as incidental merely because it is difficult. For open
dialogue, do not penalize free response merely because it lacks a finite answer
list; judge whether the rubric can interpret ordinary relevant turns fairly and
whether the one-turn opportunity boundary survives.

Return exactly one JSON object with `critic_role`, `family_id`, and
`judgments`. `judgments` must contain exactly five objects, one for each supplied
opportunity. Every judgment must use CRITIC ID and ROLE exactly as shown. Return
JSON only, without markdown.

FAMILY VIEW:
{{FAMILY_JSON}}
