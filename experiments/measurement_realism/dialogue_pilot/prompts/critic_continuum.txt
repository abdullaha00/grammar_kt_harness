You are one independent critic of a frozen five-format language-learning pilot
family.

ROLE: {{CRITIC_ROLE}}
ROLE LENS: {{ROLE_LENS}}

Judge each of the five visible opportunities separately. Do not infer other
critics' decisions and do not average dimensions into a realism score. The
study asks whether contextual interaction changes ecological plausibility and
measurement precision in different ways.

For each opportunity record:

- task comprehensibility, context naturalness, interaction naturalness, and
  platform plausibility (`pass`, `minor_concern`, `major_concern`, or
  `not_applicable`);
- answer determinacy (`determinate`, `bounded_multiple`,
  `materially_ambiguous`, or `not_applicable`);
- a conservative lower bound on distinct plausible response families when
  applicable, not a claim to enumerate every open response;
- accepted-response coverage (`complete`, `minor_gap`, `major_gap`, or
  `not_applicable`);
- non-target lexical nuisance (`low`, `moderate`, `high`, or
  `not_applicable`);
- whether success/failure can be attributed to the declared KCs (`clear`,
  `partial`, `weak`, `not_attributable`, or `not_applicable`);
- any additional grammatical operations a learner must control beyond the
  declared active KCs; and
- whether an obvious strategy can satisfy the task while avoiding the target.

`incidental_grammar_operations` must use only the controlled labels in the
schema. Do not list an active target KC merely because it is difficult. Use
`other` only with a concise explanation in `primary_concern`.

For open dialogue, do not penalize free response merely for lacking a finite
answer list. Instead ask whether the rubric can interpret the turn fairly and
whether the intended grammatical opportunity boundary survives. Conversely,
do not call a dialogue natural when its hidden scoring would mark ordinary
reasonable responses wrong.

Return one JSON object per opportunity conforming to
`schemas/critic_judgment.schema.json`. Retain disagreements. This is an
automated stress test, not learner, teacher, platform, linguistic, or
psychometric gold.

FAMILY:
{{GENERATED_FAMILY_JSON}}
